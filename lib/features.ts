export type Features = {
  pretests: boolean;
  glossary: boolean;
  resources: boolean;
  flashcards: boolean;
  progress: boolean;
  settings: boolean;
};

export const ALL_ENABLED: Features = {
  pretests: true,
  glossary: true,
  resources: true,
  flashcards: true,
  progress: true,
  settings: true,
};
